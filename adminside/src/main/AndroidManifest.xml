<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ingsoftuaz.kevin.adminside">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/RedTheme">
        <activity
            android:name=".Admin"
            android:label="@string/app_name"
            android:theme="@style/RedTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Donors"
            android:label="@string/title_activity_donors"
            android:theme="@style/RedTheme.NoActionBar" />
        <activity
            android:name=".DonorData"
            android:label="@string/title_activity_donor_data"
            android:theme="@style/RedTheme.NoActionBar"></activity>
    </application>

</manifest>